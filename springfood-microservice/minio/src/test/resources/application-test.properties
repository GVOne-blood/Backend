# Test configuration for MinIO
# These are dummy values for testing - tests should mock MinIO client

minio.endpoint=http://localhost:9000
minio.access-key=test-access-key
minio.secret-key=test-secret-key
minio.bucket=test-bucket
minio.auto-create-bucket=false
