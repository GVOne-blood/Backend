# ===================================================================
# Spring Boot configuration for the "dev" profile.
#
# This configuration overrides the application.yml file.
#
# More information on profiles: https://www.jhipster.tech/profiles/
# More information on configuration properties: https://www.jhipster.tech/common-application-properties/
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

logging:
  level:
    ROOT: DEBUG
    tech.jhipster: DEBUG
    org.hibernate.SQL: DEBUG
    com.theblood.statisticalreport: DEBUG
    # Disable noisy JMX logging
    javax.management.remote.rmi: WARN
    javax.management.remote: WARN
    # Enable OkHttp logging for debugging Carbone requests
    okhttp3.OkHttpClient: DEBUG
    okhttp3.logging.HttpLoggingInterceptor: DEBUG

management:
  zipkin: # Use the "zipkin" Maven profile to have the Spring Cloud Zipkin dependencies
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0 # report 100% of traces

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false # we use Webpack dev server + BrowserSync for livereload
  jackson:
    serialization:
      indent-output: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: ${DEFAULT_DATABASE_URL:jdbc:postgresql://localhost:5432/springfood}
    username: ${DEFAULT_DATABASE_USERNAME:postgres}
    password: ${DEFAULT_DATABASE_PASSWORD:123456}
    hikari:
      poolName: Hikari
      auto-commit: false
  liquibase:
    enabled: false
  # Remove 'faker' if you do not want the sample data to be loaded automatically

server:
  port: 8090
  # make sure requests the proxy uri instead of the server one
  forward-headers-strategy: native

# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  cache: # Cache configuration
    redis: # Redis configuration
      expiration: 3600 # By default objects stay 1 hour (in seconds) in the cache
      server: redis://localhost:6379
      cluster: false
      # server: redis://localhost:6379,redis://localhost:16379,redis://localhost:26379
      # cluster: true
  # CORS is disabled by default on microservices, as you should access them through a gateway.
  # If you want to enable it, please uncomment the configuration below.
  # cors:
  #   allowed-origins: "http://localhost:9000,https://localhost:9000"
  #   allowed-methods: "*"
  #   allowed-headers: "*"
  #   exposed-headers: "Authorization,Link,X-Total-Count"
  #   allow-credentials: true
  #   max-age: 1800
  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        base64-secret: M2FmMDY1MjRjOGM2OGUyOTk5NjAxNTFiZjFlNTRiNjkxOGNhNjU1MjA5YWMxZjQ1MDllNDkzODZhMWUwNmYwNDVmZjEwNGMwZmZjMGMxNWE4YTg2YjE4MDU1ZDc2ZGQxOTlkNDNlN2NlMmNiMTc0ZTdmY2EwOGJkMzRlN2YxMzQ=
        # Token is valid 24 hours
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      ring-buffer-size: 512
# ===================================================================
# Application specific properties
# Add your own application properties here, see the ApplicationProperties class
# to have type-safe configuration, like in the JHipsterProperties above
#
# More documentation is available at:
# https://www.jhipster.tech/common-application-properties/
# ===================================================================

# application:

# Carbone Configuration
# Option 1: Self-hosted Carbone Server (Community Edition)
carbone:
  base-url: ${CARBONE_BASE_URL:http://localhost:8100}
  template-path: ${CARBONE_TEMPLATE_PATH:statistical-report/src/main/resources/templates/reports}
  # Option 2: Carbone Cloud (SaaS - Recommended for Production)
  # Set enabled=true để dùng Carbone Cloud thay vì self-hosted
  cloud:
    enabled: ${CARBONE_CLOUD_ENABLED:true}
    api-token: ${CARBONE_CLOUD_API_TOKEN:test_eyJhbGciOiJFUzUxMiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiIxMzc3MTYxOTI2NzM1NzY2NjYzIiwiYXVkIjoiY2FyYm9uZSIsImV4cCI6MjQzNTg4NTY5NSwiZGF0YSI6eyJ0eXBlIjoidGVzdCJ9fQ.AE5Dp9AL2c8-iKecKFye7U9PO-RmV4K-qmqwzO2On9VD9IhHUPVQrLw066L3edC4VuPp1gB2Rvjy0KeNLhU5xyxVAWwNZehyUbBUi4ET_lpP1jBphLopQK8Bp7vAbIl0ifGxoscMwka5ja-AcIblbXOJv8oQA7CHfFNTyNEoNQQRkkJX}
    api-version: ${CARBONE_CLOUD_API_VERSION:4}

# MinIO Configuration
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:springfood-carbone-out}
  auto-create-bucket: true
  # Buckets riêng cho templates và reports
  template-bucket: ${MINIO_TEMPLATE_BUCKET:springfood-input}
  report-bucket: ${MINIO_REPORT_BUCKET:springfood-carbone-out}
